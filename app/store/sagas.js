/**
 *  Redux saga class init
 * Import every feature saga here
 *
 */
import { all } from 'redux-saga/effects';
// import { pinSagas } from 'app/features/pin/sagas';

// export default [loginSaga];

export default function* rootSaga() {
    // yield all([...pinSagas]);
}
